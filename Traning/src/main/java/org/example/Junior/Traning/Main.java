package org.example.Junior.Traning;

import java.util.Arrays;
import java.util.List;

public class Main {
    public static void main(String[] args) {

//        Action compareValues = Integer::compare;
//        Action sumValues = Integer::sum;
//
//        System.out.println(compareValues.action(2, 5));
//        System.out.println(sumValues.action(2, 5));

//        List<String> stringList = Arrays.asList("Hello", "World", "xD");
//        stringList.stream().filter(str -> str.length() > 3).forEach(System.out::println);

//        Arrays.asList(1, 4, 19, 32, 2, 10, 1).stream().sorted().forEach(System.out::println);

//        List<User> userList = Arrays.asList(new User("Roman", 25),
//                new User("Igor", 60), new User("Olga", 40));

//        userList.stream().map(user -> new User(user.name, user.age - 5)).
//                filter(user -> user.age <= 25).forEach(System.out::println);

//        userList.stream().sorted((x, y) -> y.age - x.age).forEach(System.out::println);
    }
}

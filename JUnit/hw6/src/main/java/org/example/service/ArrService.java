package org.example.service;

import org.example.repository.ArrLists;

import java.util.List;

public interface ArrService {
    double averageList(List<Integer> list);
    double checkValues(double num1, double num2);
}
